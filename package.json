{
  "name": "decibel-reader",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server/index.ts",
    "build": "bun build --target=bun --production --sourcemap --outdir=dist ./src/server/index.ts",
    "start": "bun run build && cd dist && bun run index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@mediapipe/tasks-audio": "^0.10.32",
    "@mediapipe/tasks-vision": "^0.10.32",
    "react": "^19",
    "react-dom": "^19",
    "react-router-dom": "^7.13.0",
    "recharts": "^3.7.0"
  }
}
